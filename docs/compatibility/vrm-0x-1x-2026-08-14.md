# VRM 0.x/1.0 Compatibility Gate Report

**Date:** 2026-08-14  
**Implementation baseline:** `4d95643`  
**Scope:** local Windows headless parser/runtime gate only

## Result

**Conditional.** The normalization implementation passes the local automated
gate for a matrix containing at least three VRM 0.x models and two VRM 1.0
models. The lifecycle replacement and physical soak evidence required for an
unconditional release gate remains pending.

No camera, viewport, MToon visual, macOS, or physical SpringBone result is
claimed from this report.

## Model inventory

| Model | SHA-256 | Preflight | Runtime |
|---|---|---|---|
| `【輪郭線あり】つくよみちゃん公式3Dモデル タイプA（マテリアル数18→7）.vrm` | `CA7A6FBF5C2DE0ACCD3373CF25F77C45632F36A863132316BA8224AD608994F2` | PASS, VRM 0.x | PASS, `Initialized`, MVP-capable |
| `IrisPart1(ShapeKey Reduce2).vrm` | `48C8D7239980896334E106D1673918BF61D17D3B9B267D7354975C282AB01EC8` | PASS, VRM 0.x | PASS, `Initialized`, MVP-capable |
| `RearAlice_3.0.vrm` | `0AF3B2976448050F1674F7121C767C1C356BF1EADE9D7A1ABC27D34E58526A84` | PASS, VRM 0.x | PASS, `Initialized`, MVP-capable |
| `1565994099520778586.vrm` | `B5A3D4126C4A30EF3BFBCFC764A24DC48511B558799D98D4C2FF1DB0BDC7AB01` | PASS, VRM 1.0 | PASS, `Initialized`, MVP-capable |
| `AvatarSample_C.vrm` | `193D614B524001C27EC401A5A3E21F03F039BEE8422A1C9006B39C0D4AA20009` | PASS, VRM 1.0 | PASS, `Initialized`, MVP-capable |
| `alicia-solid.vrm` | `79F55310C43799DBA858586F053712282D8B5FA82BDD425BDA2402736ACBDF4F` | FAIL, HTML content instead of GLB | skipped |
| `seed-san.vrm` | `23C371331C8D79538366530E860BF7458A72144844BAC10DDB59A415A50E266C` | FAIL, HTML content instead of GLB | skipped |

The root-directory run also found additional valid VRM 0.x models
`RearAliceLite_3.0.vrm` and `Sapphy.vrm`. `IrisPart1.vrm`,
`IrisPart1(ShapeKey Reduce).vrm`, and `SapphyPerfectSync.vrm` reached the
runner timeout and are not counted as passing runtime evidence. The last two
fixture files above are ignored local artifacts and begin with an HTML
`<!DOCTYPE html>` document, so they are not counted as VRM models.

## Automated evidence

| Check | Result |
|---|---|
| VRM 0.x core descriptor, expression, MToon, SpringBone unit tests | PASS |
| VRM 1.0 regression tests | PASS |
| load/replace/unload lifecycle tests | PASS |
| representative real-model matrix (3 VRM 0.x + 2 VRM 1.0) | PASS |
| local workspace compilation | PASS |
| local clippy with warnings denied | PASS |
| 20 real-model replacements | PENDING |
| bounded real-model SpringBone soak | PENDING |

Commands run:

```text
cargo fmt --all -- --check
cargo run -p xtask -- vrm-compat tests/fixtures/vrm
cargo run -p xtask -- vrm-compat .
cargo check --workspace
cargo test -p xtask
cargo test -p vtuber-app import
cargo test -p vtuber-avatar
cargo clippy -p xtask --all-targets -- -D warnings
cargo clippy -p vtuber-app --all-targets -- -D warnings
cargo clippy -p vtuber-avatar --all-targets -- -D warnings
cargo test --manifest-path vendor/bevy_vrm1/Cargo.toml
cargo clippy --manifest-path vendor/bevy_vrm1/Cargo.toml --all-targets -- -D warnings
git diff --check
```

The compatibility runner exited `2` as designed because the two HTML files
failed preflight. The full workspace run also exited `2` because three
additional ignored artifacts did not initialize within the bounded 600-frame
runner limit. Its passing results included:

```text
inore-vrm1.vrm: generation Vrm1, initialized true, mvp capable true
tsukuyomi-chan.vrm: generation Vrm0, initialized true, mvp capable true
IrisPart1(ShapeKey Reduce2).vrm: generation Vrm0, initialized true, mvp capable true
RearAlice_3.0.vrm: generation Vrm0, initialized true, mvp capable true
1565994099520778586.vrm: generation Vrm1, initialized true, mvp capable true
AvatarSample_C.vrm: generation Vrm1, initialized true, mvp capable true
```

## Platform and hardware status

| Evidence | Status |
|---|---|
| Windows local automated checks | PASS |
| Windows physical camera protocol for this issue | NOT RUN |
| Windows visual MToon/outline/alpha inspection | NOT RUN |
| Windows physical SpringBone soak | NOT RUN |
| macOS automated run | NOT RUN |
| macOS physical camera/visual run | NOT RUN |

## Follow-up required before unconditional support

- Run the requested 20 bidirectional replacements and a bounded SpringBone
  soak on real models.
- Record real Windows and macOS visual/hardware evidence separately.
